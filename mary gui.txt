-- Criação da GUI
local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local OpenButton = Instance.new("TextButton")
local CloseButton = Instance.new("TextButton")
local TeleportButton = Instance.new("TextButton")
local PlayerNameBox = Instance.new("TextBox")
local TargetNameBox = Instance.new("TextBox")
local ESPButton = Instance.new("TextButton")
local InvisibilityButton = Instance.new("TextButton")
local GodModeButton = Instance.new("TextButton")
local ScrollingFrame = Instance.new("ScrollingFrame")

-- Propriedades da GUI
ScreenGui.Name = "MyGui"
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

Frame.Name = "MainFrame"
Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) -- Cor preta
Frame.Position = UDim2.new(0.5, -100, 0.5, -150)
Frame.Size = UDim2.new(0, 200, 0, 350)

ScrollingFrame.Name = "ScrollingFrame"
ScrollingFrame.Parent = Frame
ScrollingFrame.Size = UDim2.new(1, 0, 1, 0)
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 1.5, 0)
ScrollingFrame.ScrollBarThickness = 10
ScrollingFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) -- Cor preta
ScrollingFrame.BorderColor3 = Color3.fromRGB(75, 0, 130) -- Borda roxo escuro

local function createButton(name, parent, text, position, size)
    local button = Instance.new("TextButton")
    button.Name = name
    button.Parent = parent
    button.Text = text
    button.Position = position
    button.Size = size
    button.BackgroundColor3 = Color3.fromRGB(128, 128, 128) -- Cor cinza
    button.BorderColor3 = Color3.fromRGB(75, 0, 130) -- Borda roxo escuro
    button.TextColor3 = Color3.fromRGB(255, 255, 255) -- Letras brancas
    return button
end

OpenButton = createButton("OpenButton", ScreenGui, "Abrir", UDim2.new(0.5, -50, 0.5, 120), UDim2.new(0, 100, 0, 50))
CloseButton = createButton("CloseButton", ScrollingFrame, "Fechar", UDim2.new(0.5, -50, 0.5, 100), UDim2.new(0, 100, 0, 50))
TeleportButton = createButton("TeleportButton", ScrollingFrame, "Teletransportar", UDim2.new(0.5, -50, 0.5, 0), UDim2.new(0, 100, 0, 50))
ESPButton = createButton("ESPButton", ScrollingFrame, "Ativar ESP", UDim2.new(0.5, -50, 0.5, -110), UDim2.new(0, 100, 0, 25))
InvisibilityButton = createButton("InvisibilityButton", ScrollingFrame, "Invisibilidade", UDim2.new(0.5, -50, 0.5, -140), UDim2.new(0, 100, 0, 25))
GodModeButton = createButton("GodModeButton", ScrollingFrame, "God Mode", UDim2.new(0.5, -50, 0.5, -170), UDim2.new(0, 100, 0, 25))

PlayerNameBox.Name = "PlayerNameBox"
PlayerNameBox.Parent = ScrollingFrame
PlayerNameBox.PlaceholderText = "Seu Nome"
PlayerNameBox.Position = UDim2.new(0.5, -50, 0.5, -50)
PlayerNameBox.Size = UDim2.new(0, 100, 0, 25)
PlayerNameBox.BackgroundColor3 = Color3.fromRGB(128, 128, 128) -- Cor cinza
PlayerNameBox.BorderColor3 = Color3.fromRGB(75, 0, 130) -- Borda roxo escuro
PlayerNameBox.TextColor3 = Color3.fromRGB(255, 255, 255) -- Letras brancas

TargetNameBox.Name = "TargetNameBox"
TargetNameBox.Parent = ScrollingFrame
TargetNameBox.PlaceholderText = "Nome do Alvo"
TargetNameBox.Position = UDim2.new(0.5, -50, 0.5, -80)
TargetNameBox.Size = UDim2.new(0, 100, 0, 25)
TargetNameBox.BackgroundColor3 = Color3.fromRGB(128, 128, 128) -- Cor cinza
TargetNameBox.BorderColor3 = Color3.fromRGB(75, 0, 130) -- Borda roxo escuro
TargetNameBox.TextColor3 = Color3.fromRGB(255, 255, 255) -- Letras brancas

-- Funções dos botões
OpenButton.MouseButton1Click:Connect(function()
    Frame.Visible = true
end)

CloseButton.MouseButton1Click:Connect(function()
    Frame.Visible = false
end)

TeleportButton.MouseButton1Click:Connect(function()
    local playerName = PlayerNameBox.Text
    local targetName = TargetNameBox.Text
    teleportPlayer(playerName, targetName)
end)

ESPButton.MouseButton1Click:Connect(function()
    activateESP()
end)

InvisibilityButton.MouseButton1Click:Connect(function()
    toggleInvisibility()
end)

GodModeButton.MouseButton1Click:Connect(function()
    toggleGodMode()
end)

-- Inicialmente, a Frame está invisível
Frame.Visible = false

-- Script de Teletransporte
local Players = game:GetService("Players")

function teleportPlayer(playerName, targetPlayerName)
    local player = Players:FindFirstChild(playerName)
    local targetPlayer = Players:FindFirstChild(targetPlayerName)
    
    if player and targetPlayer then
        player.Character:SetPrimaryPartCFrame(targetPlayer.Character.PrimaryPart.CFrame)
    else
        print("Jogador não encontrado")
    end
end

-- Script de ESP
function activateESP()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= game.Players.LocalPlayer then
            local highlight = Instance.new("Highlight")
            highlight.Parent = player.Character
            if player.Team == game.Players.LocalPlayer.Team then
                highlight.FillColor = Color3.new(0, 0, 1) -- Azul para aliados
            else
                highlight.FillColor = Color3.new(1, 0, 0) -- Vermelho para inimigos
            end
            highlight.OutlineColor = Color3.new(1, 1, 1)
        end
    end
end

-- Script de Invisibilidade
local isInvisible = false

function toggleInvisibility()
    local player = game.Players.LocalPlayer
    if isInvisible then
        player.Character.HumanoidRootPart.Transparency = 0
        player.Character.Head.Transparency = 0
        for _, part in pairs(player.Character:GetChildren()) do
            if part:IsA("BasePart") then
                part.Transparency = 0
            end
        end
        isInvisible = false
    else
        player.Character.HumanoidRootPart.Transparency = 1
        player.Character.Head.Transparency = 1
        for _, part in pairs(player.Character:GetChildren()) do
            if part:IsA("BasePart") then
                part.Transparency = 1
            end
        end
        isInvisible = true
    end
end

-- Script de God Mode
local isGodMode = false

function toggleGodMode()
    local player = game.Players.LocalPlayer
    if isGodMode then
        player.Character.Humanoid.MaxHealth = 100
        player.Character.Humanoid.Health = 100
        isGodMode = false
    else
        player.Character.Humanoid.MaxHealth = math.huge
        player.Character.Humanoid.Health = math.huge
        isGodMode = true
    end
end

-- Script de seta vermelha mostrando a distância do inimigo
function createArrow(target)
    local arrow = Instance.new("BillboardGui")
    arrow.Name = "Arrow"
    arrow.Parent = target.Character.Head
    arrow.Size = UDim2.new(1, 0, 1, 0)
    arrow.StudsOffset = Vector3.new(0, 3, 0)
    arrow.AlwaysOnTop = true

    local frame = Instance.new("Frame")
    frame.Parent = arrow
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundColor3 = Color3.new(1, 0, 0) -- Cor vermelha

    local textLabel = Instance.new("TextLabel")
    textLabel.Parent = frame
    textLabel.Size = UDim2.new(1, 0, 1, 0)
    textLabel.BackgroundTransparency = 1
    textLabel.TextColor3 = Color3.new(1, 1, 1) -- Cor branca
    textLabel.TextScaled = true

    game:GetService("RunService").RenderStepped:Connect(function()
        local distance = (target.Character
